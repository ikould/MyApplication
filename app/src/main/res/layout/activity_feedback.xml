<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_sample"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <RelativeLayout
        android:id="@+id/rl_top_feedback"
        android:layout_width="match_parent"
        android:layout_height="@dimen/xx_132"
        android:background="@android:color/black">

        <ImageView
            android:id="@+id/iv_sample_show_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"/>

        <TextView
            android:id="@+id/textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:text="用户反馈"
            android:textColor="@android:color/white"
            android:textSize="18sp"/>
    </RelativeLayout>

    <WebView
        android:id="@+id/wb_feedback"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/rl_top_feedback"
        android:fadingEdge="none"
        android:overScrollMode="never"/>

    <ImageView
        android:id="@+id/iv_loading_feedback"
        android:layout_width="@dimen/xx_150"
        android:layout_height="@dimen/xx_63"
        android:layout_centerInParent="true"/>
</RelativeLayout>
